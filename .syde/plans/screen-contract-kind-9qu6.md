---
approved_at: "2026-04-14T11:23:20Z"
background: 'The dashboard has no way to model UI screens in the design system. UIML (internal/uiml) already exists with a full lexer/parser/renderer stack used today only by the ''design'' entity kind. Screens — the React pages users interact with — are architecturally contracts (UI is a user-facing boundary) but the contract_kind enum has no value for them. Result: the Web SPA component has a long --files list but no per-screen contract, and there''s no drift signal when a new page lands without being modelled.'
completed_at: "2026-04-14T11:37:12Z"
created_at: "2026-04-14T11:21:56Z"
id: PLN-0012
kind: plan
name: screen-contract-kind
objective: Add 'screen' as a contract_kind and 'render' as its interaction_pattern. Every screen contract carries a 'wireframe' UIML source that parses through the existing uiml.Parse and renders via uiml.RenderHTML into a wireframe preview shown in the contract detail panel. Backfill the 13 existing SPA screens. Add a scan gate that warns when web/src/pages/*.tsx files lack a screen contract.
phases:
    - changes: 'internal/model/entity.go ContractEntity gains Wireframe string yaml:''wireframe,omitempty''. internal/model/validation.go case *ContractEntity adds: when ContractKind==''screen'', require non-empty Wireframe, run uiml.Parse on it, surface parse errors as ValidationErrors. internal/cli/add.go + update.go register --wireframe string flag and wire into ContractEntity. No changes to --contract-kind / --interaction-pattern semantics (still free-form); ''screen'' and ''render'' are documented enum values the validator only checks when they actually appear.'
      description: ContractEntity.Wireframe field, --wireframe CLI flag, validator enforces UIML parse when contract_kind=screen
      details: UIML parse errors come back as a []uiml.ParseError — map each to a ValidationError with field 'wireframe' and message containing the UIML line:col. Parser is lenient — only structural errors should block. internal/model imports internal/uiml (check for cycles — uiml imports model? probably not, it's self-contained). If cycle exists, do the parse check in internal/audit instead of model/validation.go.
      id: phase_1
      name: Model + CLI + validator for Wireframe
      notes: 'Wireframe body can be long (multi-line UIML). CLI flag syntax: --wireframe "<screen>\n  <layout...>\n</screen>" — shell heredoc friendly.'
      objective: syde add contract --contract-kind screen --wireframe '<screen>...</screen>' round-trips and fails validation on malformed UIML
      status: completed
      tasks:
        - add-wireframe-field-cli-flag-and-validator-parse-check
    - changes: 'internal/query/formatter.go FormatJSON contract case: when ContractKind==''screen'' and Wireframe != '''', parse the UIML via uiml.Parse; on success, set entityMap[''wireframe''] = e.Wireframe, entityMap[''wireframe_html''] = uiml.RenderHTML(nodes), entityMap[''wireframe_ascii''] = uiml.RenderASCII(nodes, 80). web/src/lib/api.ts contract type gains wireframe?, wireframe_html?, wireframe_ascii?. web/src/components/EntityDetail.tsx contract case renders a Wireframe section when contract_kind===''screen'' + wireframe_html present: a bordered monospace box with dangerouslySetInnerHTML.'
      description: Screen contract detail panel shows the UIML rendered as wireframe HTML via uiml.RenderHTML
      details: The UIML HTML renderer already produces self-contained markup — no extra styling needed beyond a wrapping border + background contrast. Keep the section below the standard contract fields (input/output/parameters) so the wireframe is the last thing on the page. Add a small 'View UIML source' toggle that swaps to a <pre> showing the raw source — optional polish.
      id: phase_2
      name: Dashboard wireframe render
      notes: dangerouslySetInnerHTML is acceptable here because UIML output is generated by our own Go renderer, not user-supplied HTML. Same trust model as server-rendered markdown in other panels.
      objective: Clicking a screen contract in the dashboard shows its wireframe preview in the detail panel
      status: completed
      tasks:
        - pre-render-wireframe-htmlascii-in-formatjson-entitydetail-panel
    - changes: skill/SKILL.md contract rules add 'screen' to the contract_kind enum with 'render' as the paired interaction_pattern. New subsection '### Screen contracts' with a worked example using a small UIML body. skill/references/entity-spec.md Contract table adds 'wireframe' row with link to the UIML syntax section. skill/references/commands.md contract flag reference adds --wireframe.
      description: SKILL.md, entity-spec.md, commands.md document screen contracts, the render pattern, and the --wireframe flag
      details: 'Skill example: a tiny Concepts List screen with sidebar, main list, and detail panel expressed in UIML. Cross-link to ''syde design create'' for the existing UIML-based design kind so readers understand screen contracts use the same language.'
      id: phase_3
      name: Skill docs
      notes: No change to UIML syntax — the skill just points at the existing design-kind language.
      objective: An agent reading the skill can create a screen contract with a valid UIML wireframe on the first try
      status: completed
      tasks:
        - document-screen-render-wireframe-in-the-skill
    - changes: 'Shell script /tmp/syde-screens.sh with 13 ''syde add contract --contract-kind screen --interaction-pattern render'' calls. Screens: Overview, File Tree, Graph, Plan View, Task Board, Learning Feed, Concepts ERD, Systems Inbox, Components Inbox, Contracts Inbox, Concepts Inbox (list), Flows Inbox, Decisions Inbox. Each with --input (route path), --input-parameter (param list), --output (rendered UI), --output-parameter, --wireframe (UIML matching the layout), --add-rel web-spa:references, --add-rel syded-dashboard:belongs_to, and --file pointing at the Tsx page + any shared component files.'
      description: Create 13 screen contracts for every existing React view with UIML wireframes matching their real layout
      details: 'UIML for each screen describes the actual layout: Overview uses a grid of metric cards; File Tree uses a split left-tree/right-content layout; Graph/ERD use a single canvas region; PlanView/TaskBoard use column stacks; kind inbox views share the 2-column list+detail pattern. Keep each wireframe under ~40 lines of UIML — enough to convey structure, not a full UI spec.'
      id: phase_4
      name: Backfill SPA screens
      notes: This is the rare case where script-based bulk creation is appropriate (per the SKILL.md bulk entity creation guidance). 13 contracts in one shell script beats 13 individual tool calls.
      objective: Every page under web/src/pages and every kind-scoped inbox view has a corresponding screen contract the scan gate is happy with
      status: completed
      tasks:
        - backfill-13-screen-contracts-for-existing-spa-routes
    - changes: internal/audit/screens.go new file with screenFindings(all, tree) function. Walks tree nodes under web/src/pages/*.tsx, for each non-ignored one check if any ContractEntity with ContractKind=='screen' has the path in its Files. If no owner, emit a WARN Finding with Category 'screen_unclaimed'. Integrated into audit.Run alongside planPhaseFindings and conceptFindings.
      description: internal/audit/screens.go warns on web/src/pages/*.tsx files with no owning screen contract
      details: Only WARN severity for v1 — don't block session end if a dev is mid-feature and hasn't landed the screen contract yet. Users can promote to ERROR in a future pass. Category constant CatScreenUnclaimed added to audit.go. Skips .test.tsx and .stories.tsx suffixes so future test/story files don't trip the gate.
      id: phase_5
      name: Scan gate for unclaimed page files
      notes: 'Complementary to the orphan-file gate (which is kind-agnostic). This one is screen-specific: it catches files owned by a non-screen component (like Web SPA) that should ALSO have a screen contract.'
      objective: syde sync check surfaces a WARN for every page tsx that lacks a screen contract claiming it via --files
      status: completed
      tasks:
        - add-screen-drift-warn-to-sync-check
plan_status: completed
relationships:
    - label: requirement
      target: existing-syde-model-baseline-hcvj
      type: references
    - target: syde-5tdt
      type: belongs_to
scope: 'In-scope: (1) ContractEntity.Wireframe field + CLI --wireframe flag + validator (require wireframe when contract_kind=screen + parse through uiml.Parse); (2) FormatJSON emits wireframe + wireframe_html + wireframe_ascii for screen contracts; (3) EntityDetail contract panel renders wireframe_html in a contained box when kind=screen (dangerouslySetInnerHTML, no interactivity); (4) skill docs (SKILL.md, entity-spec.md, commands.md) document screen + render + --wireframe; (5) backfill 13 screen contracts via a shell script with UIML sources; (6) internal/audit/screens.go warns on unclaimed web/src/pages/*.tsx files via sync check. Out-of-scope: interactive wireframe components, React state/events on wireframes, styled real-UI preview, bulk-import from App.tsx routes, auto-generated UIML from existing React, PreToolUse hard-blocking on missing screen contracts, design-entity screen merging.'
slug: screen-contract-kind-9qu6
source: manual
updated_at: "2026-04-14T11:37:12Z"
---
